# Об'єктно-орієнтований проект: Система управління прем'єрами

## Опис проекту

Імітаційний прототип системи управління відеопрем'єрами, розроблений на мові Java з використанням принципів об'єктно-орієнтованого програмування.

### Основні ролі системи:
- **Viewer (Глядач)** - користувач, який переглядає контент і залишає коментарі
- **Author (Автор)** - користувач, який планує прем'єри та опублікує контент
- **Moderator (Модератор)** - користувач, який модерує контент та чат
- **Content** - відеоконтент з метаданими

## Архітектура і принципи ООП

Проект демонструє наступні ключові концепції ООП:

### 1. **Наслідування (Inheritance)**
- Клас `User` - базовий клас для всіх типів користувачів
- Класи `Viewer`, `Author`, `Moderator` успадковують `User`

### 2. **Інкапсуляція (Encapsulation)**
- Приватні атрибути класів з захистом доступу
- Публічні методи (getters/setters) для управління станом об'єктів

### 3. **Поліморфізм (Polymorphism)**
- Реалізація інтерфейсів `Engageable` та `Moderatable`
- Перевизначення абстрактного методу `performAction()`

### 4. **Абстракція (Abstraction)**
- Абстрактний клас `User` визначає контракт для всіх користувачів
- Інтерфейси визначають поведінку без реалізації

### 5. **Агрегація (Aggregation)**
- Клас `PremierSystem` керує колекціями користувачів та контенту

## Структура проекту

```
OOP/
├── src/com/premiere/
│   ├── Main.java              # Точка входу в програму
│   ├── User.java              # Абстрактний базовий клас
│   ├── Viewer.java            # Клас глядача
│   ├── Author.java            # Клас автора
│   ├── Moderator.java         # Клас модератора
│   ├── Content.java           # Клас контенту
│   ├── Engageable.java        # Інтерфейс для взаємодії
│   ├── Moderatable.java       # Інтерфейс для модерації
│   └── PremierSystem.java     # Система управління
├── bin/                       # Скомпільовані class-файли
├── docs/
│   ├── requirements.md        # Вимоги до проекту
│   ├── class.puml             # UML діаграма класів
│   ├── usecase.puml           # UML діаграма варіантів використання
│   ├── sequence.puml          # UML діаграма послідовності
│   └── state.puml             # UML діаграма станів
├── .gitignore                 # Файли, що ігноруються Git'ом
└── README.md                  # Цей файл
```

## Вимоги

- Java JDK 8 або вищої версії
- Bash або аналогічний shell

## Компіляція

Для компіляції проекту використовуйте команду:

```bash
javac -d bin src/com/premiere/*.java
```

Після компіляції всі class-файли будуть розташовані в директорії `bin/`.

## Запуск

Для запуску програми виконайте:

```bash
java -cp bin com.premiere.Main
```

Програма виведе список всіх операцій та статистику системи.

## Приклад виконання

При запуску програма демонструє:

1. **Реєстрацію користувачів** - додавання глядачів, автора та модератора
2. **Публікацію контенту** - створення та опублікування відеоматеріалів
3. **Планування прем'єри** - автор планує прем'єру та запускає чат
4. **Взаємодію глядачів** - перегляд контенту та коментування
5. **Модерацію контенту** - блокування спаму та видалення неприпустимих коментарів
6. **Демонстрацію поліморфізму** - виклик методів через базовий клас User
7. **Апеляцію на модеративне рішення** - подання скарги автором
8. **Статистику системи** - вивід всієї інформації про користувачів та контент

## Git commit

Для зберігання змін в репозиторії (без class-файлів):

```bash
git add .
git commit -m "Додано імітаційний прототип системи управління прем'єрами"
git push
```

### .gitignore

Файл `.gitignore` налаштований на ігнорування:
- `*.class` - всіх Java class-файлів
- `bin/` - директорії компільованих файлів
- `out/` та `target/` - директорій компіляції IDE
- `.idea/` та `.vscode/` - конфігурацій IDE
- `*.jar` та `*.war` - архівів

## Автор

Розроблено як імітаційний прототип об'єктно-орієнтованого проекту.

## Ліцензія

Проект розповсюджується як освітній матеріал.
